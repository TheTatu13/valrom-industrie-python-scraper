"""peviitor-scraper-py — a self-healing job scraper skeleton for peviitor.ro.

The Python counterpart of the *-nodejs-scraper template. Same output contract
(jobs upserted to api.peviitor.ro), same self-healing selector cascade, plus an
optional adaptive layer via Scrapling.
"""

__version__ = "0.1.0"
