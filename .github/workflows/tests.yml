name: Tests

# CANONICAL CALLER PATTERN from the Brewtality-3-16 template — do not add
# test logic here. Changes belong in tests-reusable.yml in the template.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  tests:
    uses: TheTatu13/Brewtality-3-16/.github/workflows/tests-reusable.yml@v1
